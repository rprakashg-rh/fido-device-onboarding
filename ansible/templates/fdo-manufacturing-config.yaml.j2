host_ip: "{{ host_ip }}"
port: "{{ port }}"
manufacturing_key: "{{ manufacturing_key }}"
owner_ca_cert: "{{ owner_ca_cert }}"
device_ca_key: "{{ device_ca_key }}"
device_ca_cert: "{{ device_ca_cert }}"
db: "{{ db }}"